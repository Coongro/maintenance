/**
 * Orden de trabajo — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `orden-de-trabajo.view.ts`,
 * datos en `use-orden-de-trabajo.ts`, lógica custom en `handlers.ts`.
 */
export { OrdenDeTrabajoView } from './orden-de-trabajo.view.js';
