/**
 * Mantenimiento — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `mantenimiento.view.ts`,
 * datos en `use-mantenimiento.ts`, lógica custom en `handlers.ts`.
 */
export { MantenimientoView } from './mantenimiento.view.js';
